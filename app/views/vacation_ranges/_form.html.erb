<%= form_with(model: vacation_range) do |form| %>
  <% if vacation_range.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(vacation_range.errors.count, "error") %> prohibited this vacation_range from being saved:</h2>

      <ul>
        <% vacation_range.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :start_date, style: "display: block" %>
    <%= form.datetime_field :start_date %>
  </div>

  <div>
    <%= form.label :end_date, style: "display: block" %>
    <%= form.datetime_field :end_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
